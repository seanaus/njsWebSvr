{{!-- <div class="carouselContainer">
    {{#each carousel.items}}
		<div class="slides fade">
			<div class="numbertext">
                {{ currentSlide @index }} / {{ ../slides }}
			</div>
			<img src="{{ this.image }}" class="slide">
			<div class="text">
				{{ this.caption }}
			</div>
		</div>
	{{/each}}
</div> --}}
<div class="carousel">
    {{#each carousel.items}}
        {{!-- <div class="numbertext">
            {{ currentSlide @index }} / {{ ../slides }}
        </div> --}}
        <div class="slide kenburns-top-right" 
                style="background-image: url('{{this.image}}')">
        </div>
        {{!-- <div class="text">
            {{ this.caption }}
        </div> --}}
	{{/each}}
</div>
