<div class="burger-container">
    <div class="burger">
        <div class="burger-line burger-line-top"></div>
        <div class="burger-line burger-line-middle"></div>
        <div class="burger-line burger-line-bottom"></div>
    </div>
</div>
<div class="navBar" id="navBar">
    <p>{{auth}}</p>
    {{#each navbar.items}}
    <a class="navPill navLink{{#if this.visible}}showMenuButton{{else}}hideMenuButton{{/if}}" href={{this.route}}>
        <div class=" material-icons icon">
            {{this.matIcon}}
        </div>
        <div class="iconLabel">
            {{this.caption}}
        </div>
    </a>
    {{/each}}
</div>