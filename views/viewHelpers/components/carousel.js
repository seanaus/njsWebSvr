const curentSlide =  (value) => {
    return parseInt(value) + 1;
}
const applyStyle =  (config) => {
    switch(config.id) {
        case 1:
        case 2:
        default:
    }
    return parseInt(value) + 1;
}
module.exports = {
    curentSlide
}